@model IEnumerable<WebApplication1.Models.LoaiTin>
            @foreach (var item in Model)
            {
                <li>
                    <a href="@Url.Action("Tintheotheloai","TinTuc",new{id=item.LoaiTinID, metatitle=item.TenLoaiTin})">
                        <span style="color:white; font:status-bar;font-weight:bold">@item.TenLoaiTin</span>
                    </a>
                </li>
                
            }